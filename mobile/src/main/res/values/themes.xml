<resources>
    <!-- Base application theme. -->
    <style name="Theme.DNDSync" parent="Theme.Material3.DynamicColors.Light.Rikka">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
    </style>
</resources>